[pytest]
testpaths = tests
addopts =
    -ra
    --strict-markers
    --cov=app
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=100
filterwarnings =
    ignore::DeprecationWarning
